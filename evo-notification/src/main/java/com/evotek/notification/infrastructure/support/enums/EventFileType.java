package com.evotek.notification.infrastructure.support.enums;

public enum EventFileType {
    INLINE, //Insert an inline attachment into body message
    ATTACHMENT, //Insert an attachment into body message
}
